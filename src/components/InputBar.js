import React from 'react';


// function to display the input bar where the user can enter the values.
const InputBar = ({ value }) => {
  return (
    <div className="display">
      {value}
    </div>
  );
};

export default InputBar;